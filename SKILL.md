---
name: floor-plan-generator
description: 根据上传图片或房间尺寸识别墙体、门窗、尺寸和房间名称，并生成、修改或分析建筑户型平面图及 CAD 图纸。
---

# 建筑户型平面图生成 Skill

## 1. Skill 简介

本 Skill 用于根据用户上传的建筑户型图片、草图、手绘图、参考图或文字尺寸描述，识别并生成规范的**建筑户型平面图**。

核心目标是将用户提供的非标准户型信息转换为结构清晰、比例合理、具有建筑制图表达特征的二维平面图。

本 Skill 尤其适用于：

- 根据上传的户型图片重新绘制建筑平面图
- 根据手绘草图生成规范户型图
- 根据参考图片识别房间布局
- 根据房间尺寸描述生成户型图
- 对已有户型图进行规范化重绘
- 输出具有墙体、门、窗、房间名称及尺寸信息的建筑平面图

---

## 2. 核心要求

### 2.1 墙体必须使用双线墙

所有建筑墙体必须使用**双线墙体表达**。

禁止使用单根线条代替墙体。

墙体应由两条平行线构成，并根据实际或推定墙厚保持合理间距。

例如：

```text
错误：

────────────

正确：

════════════
```

如果输入图片能够识别墙体厚度，应优先按照图片中的实际墙厚绘制。

如果无法准确识别墙厚，可以根据建筑类型进行合理推定。

默认参考值：

- 外墙：根据图片情况确定
- 内墙：根据图片情况确定
- 特殊承重墙：根据图片或用户要求确定

用户明确指定墙厚时，必须优先使用用户提供的墙厚。

---

## 3. 输入方式

本 Skill 支持两种主要输入方式。

### 3.1 图片输入

用户可以上传：

- 建筑户型图
- CAD 截图
- 房屋平面图
- 手绘户型草图
- 房屋测绘草图
- 建筑设计草图
- 房地产户型宣传图
- 其他能够判断空间布局的图片

#### 图片预处理（必须先执行）

收到图片后，先运行本 Skill 的 `scripts/processing.py`，不得直接跳到 CAD
生成。使用当前 Skill 文件夹解析脚本路径，并为每次任务指定独立输出目录：

```powershell
python "<skill目录>\scripts\processing.py" "<输入图片>" --output-dir "<任务输出目录>\preprocessed"
```

脚本依赖 Python 3.10+、`opencv-python` 和 `numpy`。需要转写中文房间名称及
尺寸数字时，还需安装 Tesseract（含 `chi_sim` 语言包）和 `pytesseract`；未安装
OCR 时，脚本仍会输出文字候选框，`result.json` 中的 `text` 为 `null`，不得把候选
区域臆测成确定文字。

预处理输出包括：

- `result.json`：建筑范围、墙段、门合页、门半径、开启方向、窗候选、房间文字与尺寸候选
- `overlay.png`：彩色检测框叠加图，用于生成前目视核对
- `masks/walls.png`：粗黑填充墙体
- `masks/doors.png`：门扇开启弧线
- `masks/windows.png`：墙洞内成组平行窗线
- `masks/dimensions.png`：墙外尺寸线、尺寸界线、刻度和数字区域
- `masks/room_labels.png`：建筑内部房间名称候选区域
- `masks/other.png`：尚未分类的线条，供复核而不是直接丢弃

读取 `result.json` 和 `overlay.png` 后再建立几何模型。门的
`wall_orientation` 表示门洞所在墙的方向，`swing_direction` 表示门扇开启象限；
二者必须结合叠加图复核。`confidence` 较低或与其他类别重叠的候选应标记为
`INFERRED`，不能当作确定结果。

系统需要分析图片中的：

- 建筑外轮廓
- 外墙
- 内墙
- 房间分区
- 门
- 窗
- 楼梯
- 阳台
- 卫生间
- 厨房
- 客厅
- 卧室
- 走廊
- 入口
- 柱体
- 结构构件
- 尺寸标注
- 房间名称
- 其他可识别建筑元素

### 3.2 文字输入

用户也可以直接提供房间尺寸及建筑要求。

例如：

```text
生成一个两室一厅户型图

客厅：6000×4500
主卧：4500×3600
次卧：3600×3300
厨房：3000×2400
卫生间：2400×1800

外墙：根据图片情况确定
内墙：根据图片情况确定
```

系统需要根据这些尺寸合理组织空间布局。

---

## 4. 基本使用

本 Skill 支持以下两种基本使用方式：

#### 方式一：上传图片生成建筑户型平面图

用户直接上传建筑户型图片，并提出类似要求：

```text
请根据这张图片生成建筑户型平面图。
```

或者：

```text
按照这张图片重新绘制户型图，墙体使用双线墙。
```

系统应自动：

1. 分析上传图片。
2. 识别建筑外轮廓。
3. 识别墙体位置。
4. 判断墙体厚度。
5. 识别房间布局。
6. 识别门窗位置。
7. 识别尺寸及文字。
8. 建立建筑平面几何关系。
9. 使用双线墙重新绘制。
10. 输出规范建筑户型平面图。

如果图片存在透视、倾斜或比例失真，应先进行几何关系判断，再建立平面图。

#### 方式二：根据文字尺寸生成建筑户型平面图

用户可以直接提供房间及尺寸，例如：

```text
生成一个两室一厅户型图

客厅：6000×4500
主卧：4500×3600
次卧：3600×3300
厨房：3000×2400
卫生间：2400×1800

墙厚：根据图纸情况自行确定
```

系统应根据用户提供的数据合理规划空间关系，并生成建筑平面图。

---

## 5. 图片分析流程

当用户上传图片时，应按照以下流程进行分析。

### 5.1 建筑轮廓识别

首先识别：

- 建筑整体边界
- 凹凸关系
- 转角
- 阳台
- 露台
- 门廊
- 外部楼梯
- 附属空间

建立建筑整体轮廓。

### 5.2 墙体识别

识别所有可见墙体。

重点判断：

- 墙体中心位置
- 墙体两侧边界
- 墙体厚度
- 墙体连接关系
- 墙体转角
- 墙体交叉
- 墙体端点

禁止将图片中的墙体简单转换为单线。

最终生成时必须恢复为双线墙体结构。

### 5.3 房间识别

识别墙体围合形成的空间。

尽可能判断：

- 客厅
- 餐厅
- 厨房
- 主卧
- 次卧
- 卫生间
- 书房
- 储藏室
- 阳台
- 玄关
- 走廊
- 楼梯间
- 设备间

如果图片中存在房间名称，应优先采用图片文字。

### 5.4 门识别

识别：

- 单开门
- 双开门
- 推拉门
- 入户门
- 阳台门
- 卫生间门
- 其他门洞

平开门应表达：

- 门洞
- 门扇
- 开启方向
- 开启弧线

不得只使用一条短线代替门。

### 5.5 窗识别

识别：

- 普通窗
- 落地窗
- 飘窗
- 转角窗
- 推拉窗
- 其他窗洞

窗必须位于墙体内部。

绘制窗户时，应正确处理中断的墙体结构。

### 5.6 尺寸识别

如果图片存在尺寸标注，应尽可能读取并利用这些尺寸建立比例关系。

优先级：

1. 用户明确提供的尺寸
2. 图片中明确标注的尺寸
3. 根据多个已知尺寸计算得到的尺寸
4. 根据建筑比例推定的尺寸

---

## 6. 比例校准

如果图片中存在至少一个明确尺寸，应将其作为比例基准。

例如图片标注：

```text
客厅开间：4200 mm
```

则可以利用该尺寸估算：

```text
像素距离 → 实际毫米
```

建立：

```text
scale = real_length / pixel_length
```

随后利用比例关系估算其他空间尺寸。

如果图片存在多个尺寸，应进行交叉校验，避免单一尺寸识别错误导致整个户型比例失真。

---

## 7. 信息可信度

在分析过程中，将信息分为三种类型。

### USER

用户明确提供的信息。

例如：

```text
USER:
外墙厚度 = 240 mm
```

该信息具有最高优先级。

### DETECTED

从图片中明确识别出的信息。

例如：

```text
DETECTED:
客厅宽度 = 4200 mm
```

### INFERRED

根据图片比例或建筑常识推定的信息。

例如：

```text
INFERRED:
卫生间宽度 ≈ 1800 mm
```

不得将 INFERRED 信息描述成用户明确提供或图片明确标注的信息。

信息优先级：

```text
USER > DETECTED > INFERRED
```

---

## 8. 几何建模原则

生成户型图之前，应先建立内部几何模型。

推荐采用统一坐标系统：

```text
原点：(0,0)
单位：mm
```

所有建筑元素使用真实尺寸坐标表示。

例如：

```text
Wall:
start = (0,0)
end   = (6000,0)
thickness = 240
```

房间：

```text
Room:
name = 客厅
x = 240
y = 240
width = 6000
height = 4500
```

门：

```text
Door:
wall = wall_01
position = 1200
width = 900
opening = inward
```

窗：

```text
Window:
wall = wall_02
position = 1800
width = 1500
```

---

## 9. 墙体生成规则

墙体是本 Skill 最重要的建筑元素。

必须满足以下要求。

### 9.1 双线表达

任何墙体：

```text
Wall
```

必须转换为具有实际厚度的几何对象，而不是单根中心线。

### 9.2 墙体连接

墙体连接处必须正确处理：

- L 型连接
- T 型连接
- 十字连接
- 转角
- 外墙与内墙连接

不得出现明显断裂。

### 9.3 门窗开洞

门窗所在墙体必须形成真实洞口。

禁止：

```text
先画完整墙体
+
在墙体上直接叠加门窗符号
```

正确方式：

```text
墙体
→ 创建洞口
→ 放置门窗
```

---

## 10. 建筑制图表达

最终平面图应尽量符合建筑平面图的常见表达方式。

建议包括：

- 双线墙体
- 墙体厚度
- 门洞
- 门扇
- 开门弧线
- 窗洞
- 窗框
- 楼梯
- 房间名称
- 必要尺寸
- 建筑外轮廓
- 必要的结构构件

图纸应保持：

- 线条清晰
- 比例统一
- 房间边界明确
- 门窗位置准确
- 无明显几何重叠
- 无悬空墙体
- 无错误门洞

### 10.1 输出方式：直接绘制到当前 CAD 活跃文档

生成结果**不落盘为 DXF/DWG 文件**。

应通过 COM 接口连接用户当前打开的 CAD 程序（AutoCAD 或兼容 CAD），将墙体、门窗、文字等图元直接绘制到当前活跃文档的模型空间。

`scripts/generate.py` 即按此方式工作：

- 使用 `win32com.client.GetActiveObject` 连接正在运行的 CAD（依次尝试 AutoCAD.Application / Gcad.Application / ZWCAD.Application）
- 在活跃文档中自动创建 `WALLS` / `INNER_WALLS` / `DOORS` / `WINDOWS` / `DIMENSIONS` / `ROOM_NAMES` / `TEXT` 图层，已存在则复用
- 绘制完成后执行 `ZoomExtents` 刷新视图，不调用任何保存命令，由用户自行决定是否保存文档

### 10.2 文字字体要求（禁止 ??? 乱码）

任何文字（房间名称、面积、尺寸、标题等）在最终图纸上**必须可正常识别，严禁出现 `???` 或乱码**。

要求：

- 所有文字必须使用支持中文的文字样式绘制，不得使用仅含西文字形（如 `txt.shx`）的默认样式直接输出中文
- 优先使用国标大字体组合：`gbenor.shx` + `gbcbig.shx`
- 若 CAD 无国标字体，回退 TrueType 中文字体（如 `simsun.ttc` 宋体）
- `scripts/generate.py` 通过专用样式 `FP-TEXT` 自动完成上述配置，并在找不到任何中文字体时输出警告
- 生成完毕后应目视核对图纸中所有中文文字均正常显示

---

## 11. 不确定信息处理

如果图片模糊或部分区域无法判断，不得凭空制造复杂结构。

应首先根据：

1. 已知墙体
2. 建筑连续性
3. 对称关系
4. 房间功能
5. 常见住宅设计逻辑

进行合理推断。

无法可靠判断时，可以标记：

```text
INFERRED
```

如果该信息会显著影响最终户型结构，应向用户说明不确定区域。

---

## 12. 图片与文字冲突处理

如果用户文字要求与图片存在冲突，按照以下优先级执行：

```text
用户明确要求
>
图片明确尺寸
>
图片几何关系
>
系统推断
```

例如：

图片看起来外墙约为 200 mm。

但用户明确要求：

```text
外墙统一使用240mm。
```

则最终使用：

```text
240 mm
```

---

## 13. 输出前检查

生成最终户型图之前，必须进行检查。

### 墙体检查

- 是否全部使用双线墙
- 墙厚是否一致
- 墙体连接是否正确
- 是否存在断墙
- 是否存在重复墙

### 门窗检查

- 门是否位于墙体上
- 门洞是否真实断开
- 门开启方向是否合理
- 窗是否位于墙体内
- 窗洞是否正确

### 空间检查

- 房间是否形成完整闭合空间
- 是否存在异常狭窄空间
- 是否存在墙体穿过房间
- 房间关系是否与原图一致

### 比例检查

如果存在已知尺寸：

```text
重新计算关键尺寸
```

确保整体比例没有明显偏差。

---

## 14. 生成原则

当用户要求：

```text
根据这张图片生成建筑户型平面图
```

不要仅仅生成一张“看起来类似”的户型示意图。

目标应是：

```text
识别原图
↓
理解空间关系
↓
建立几何模型
↓
恢复墙体厚度
↓
创建门窗洞口
↓
绘制双线墙
↓
生成规范建筑平面图
```

必须尽可能保持：

- 原始建筑轮廓
- 房间数量
- 房间位置
- 房间比例
- 墙体关系
- 门窗位置
- 交通关系

---

## 15. 禁止事项

禁止：

- 使用单线代替墙体
- 忽略墙体厚度
- 随意改变房间数量
- 随意改变建筑外轮廓
- 将门窗放置在墙体之外
- 门洞与墙体不对应
- 明明存在尺寸却完全依靠视觉猜测
- 将推测尺寸描述为准确尺寸
- 为追求视觉效果而破坏建筑几何关系
- 窗与墙体重叠
- 任何文字显示为 `???` 或不可识别的乱码

---

## 16. 默认参数

用户没有指定时，可以采用以下默认参数：

```text
单位：mm

外墙厚度：根据图片情况确定
内墙厚度：根据图片情况确定

普通卧室门：900
卫生间门：800
入户门：1000～1200

普通窗宽：1200～1800
```

这些参数仅作为缺失信息时的参考。

如果图片能够提供更可靠的信息，应优先按照图片处理。

---

## 17. 最终目标

本 Skill 的最终目标不是简单进行“图片转图片”，而是完成：

```text
建筑图片 / 草图 / 尺寸描述
            ↓
       建筑信息识别
            ↓
       几何关系重建
            ↓
       双线墙体建模
            ↓
       门窗洞口生成
            ↓
      建筑平面图输出
```

最终生成结果应具有清晰、规范、可识别的建筑平面图特征。

其中最重要的强制要求为：

**所有墙体必须采用双线墙体表达，不允许使用单线墙代替建筑墙体。**
