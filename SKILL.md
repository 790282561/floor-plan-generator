---
name: floor-plan-generator
description: 根据上传图片或房间尺寸识别墙体、门窗、尺寸和房间名称，并生成、修改或分析建筑户型平面图及 CAD 图纸。
---

# 建筑户型平面图生成 Skill

## 1. Skill目标
### 1.1 首要目标
将户型图片、草图、CAD 截图或文字尺寸转换为结构清晰、比例合理的建筑户型平面图。必须建立几何模型，恢复墙体实际厚度，使用双线墙，并生成真实门窗洞口。
### 1.2 适用范围
适用于户型图重绘、草图规范化、房间布局识别、文字尺寸生成、门窗及尺寸分析和 CAD 输出。

## 2. 强制执行流程
### 2.1 输入解析与图片预处理
图片输入时必须先运行 `scripts/processing.py`，再读取 `result.json`、`overlay.png` 和各类 masks；未安装 OCR 时不得把文字候选框当作确定文字。
### 2.2 建筑外轮廓识别
识别整体边界、凹凸关系、转角、阳台、露台、门廊和附属空间。
### 2.3 墙体识别
识别墙体两侧边界、中心位置、厚度、端点、转角、交叉和连接关系，禁止简化为单线。
### 2.4 墙体拓扑重建
重建 L 型、T 型、十字型、转角及外墙与内墙连接，保证连续无断裂。
### 2.5 墙体断口识别
识别门洞、窗洞和其他开口，并记录为墙体上的真实断口。
### 2.6 门洞识别
识别单开门、双开门、推拉门、入户门、阳台门和卫生间门，门洞必须对应墙体。
### 2.7 门扇识别
表达门扇、铰链、宽度和开启弧线，不得只用短线表示门。
### 2.8 门开启方向识别
结合 `wall_orientation`、`swing_direction` 和 `overlay.png` 判断，无法确认时标记 `INFERRED`。
### 2.9 窗识别
识别普通窗、落地窗、飘窗、转角窗和推拉窗。窗必须位于墙体内部并形成真实窗洞。
### 2.10 尺寸与比例校准
优先级为 USER > 图片明确尺寸 > 计算值 > 推定值；已知尺寸时使用 `scale = real_length / pixel_length` 并交叉校验。
### 2.11 置信度判断
低置信度或重叠候选标记为 `INFERRED`，不得当作确定结果。
### 2.12 建筑规则校核
校核外轮廓、墙体连续性、房间闭合性、门窗位置、比例、墙厚和交通关系。
### 2.13 生成结构化数据
使用原点 `(0,0)`、单位 `mm`，生成 Wall、Opening、Door、Window、Room 和 UncertainObject。
### 2.14 CAD MCP绘制
先绘制图层、双线墙和真实洞口，再绘制门窗、文字和尺寸；不得绕过识别和校核直接凭视觉猜画。
### 2.15 绘后检查
检查双线墙、墙厚、连接、门窗位置、真实洞口、房间闭合和尺寸比例。
### 2.16 局部纠错
只修正对应局部对象，并重新检查相邻墙体、门窗、房间和尺寸关系。

## 3. 数据模型
### 3.1 Wall
包含 `id`、起终点、`thickness`、墙体类型、开口列表、来源和置信度。
### 3.2 Opening
包含 `wall_id`、位置、宽度、开口类型、来源和置信度。
### 3.3 Door
包含门洞、类型、铰链、宽度、墙方向、开启方向和开启弧线。
### 3.4 Window
包含窗洞、类型、位置、宽度和窗框线。
### 3.5 Room
包含名称、坐标、宽度、高度、边界墙列表、来源和置信度。
### 3.6 UncertainObject
记录模糊或未确认对象、候选几何、原因、置信度和 `INFERRED` 状态。

## 4. 信息可信度
### 4.1 USER
用户明确提供的信息，优先级最高。
### 4.2 DETECTED
从图片明确识别的信息。
### 4.3 INFERRED
根据比例、连续性、对称关系或建筑常识推定的信息，必须显式标记。
### 4.4 Confidence
低置信度候选必须进入不确定对象流程。优先级为 `USER > DETECTED > INFERRED`。

## 5. 建筑几何规则
### 5.1 双线墙规则
所有墙体必须由两条平行边界线构成，并具有实际墙厚，禁止单线墙。
### 5.2 墙体连接规则
正确处理 L、T、十字连接和转角，不得断裂、错位或悬空。
### 5.3 门洞规则
先切出真实墙体洞口，再放置门扇和开启弧线。
### 5.4 门扇规则
表达门扇实际宽度、铰链和开启状态。
### 5.5 门开启方向规则
开启方向由铰链、门扇和弧线共同表达，不确定时标记 `INFERRED`。
### 5.6 窗洞规则
窗洞必须中断墙体，窗框使用成组平行线，禁止窗墙重叠。
### 5.7 房间闭合规则
房间必须由连续墙体围合形成闭合空间，尽量保持原图房间数量、位置和比例。

## 6. CAD绘制规则
### 6.1 图层
分别组织墙体、门、窗、文字、尺寸和辅助线图层。
### 6.2 墙体
使用有实际厚度的双线几何对象，先处理连接和开洞。
### 6.3 门
绘制真实门洞、门扇、铰链和开启弧线。
### 6.4 窗
绘制真实窗洞和窗框平行线。
### 6.5 文字
优先使用用户输入或图片明确文字，不确定文字不得擅自补全。
### 6.6 尺寸
按已知尺寸建立比例并绘制必要尺寸，推定值不得标为精确实测值。

## 7. 纠错机制
采用“识别—建模—校核—局部修正—再次校核”闭环。无法可靠判断时保留候选并标记 `INFERRED`，不得凭空制造复杂结构。

## 8. 输入方式与使用示例
图片输入：先运行 `scripts/processing.py`，再根据结果生成双线墙户型图。

文字输入示例：
```text
生成一个两室一厅户型图
客厅：6000×4500
主卧：4500×3600
次卧：3600×3300
外墙厚度：240
```

## 9. 默认参数
单位为 `mm`；外墙和内墙厚度根据图片或用户要求确定；普通卧室门 900，卫生间门 800，入户门 1000～1200，普通窗宽 1200～1800。

## 10. 禁止事项
- 使用单线代替墙体
- 忽略墙厚或随意改变房间、外轮廓
- 未预处理图片就直接 CAD 生成
- 把推定信息当成确定信息
- 将门窗放在墙体之外或只叠加符号而不创建真实洞口
- 门洞与墙体不对应、窗与墙体重叠
