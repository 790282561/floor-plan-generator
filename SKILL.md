---
name: floor-plan-generator
description: 分阶段生成并校准户型墙体、窗户和门，保持墙体为闭合多段线，并生成真实窗洞、门洞、平开门和推拉门 CAD 图形。
---

# 户型墙体、窗户与门生成

本 Skill 用于根据户型图片或明确尺寸生成可编辑 CAD 图纸。流程必须分阶段执行：先建立并校验墙体，再生成窗户，最后生成门。任一后续阶段都不得破坏前一阶段已通过的墙体闭合拓扑。

## 当前范围

支持以下内容：

- 具有真实墙厚的双线墙体；
- 全部由闭合 `LWPOLYLINE` 表达的墙体边界；
- 根据图片证据生成窗洞和窗框；
- 根据阶段图片差异生成真实门洞、平开门扇及开启弧；
- 根据阶段图片差异生成推拉门双扇线框；
- 总体尺寸及可靠的墙体尺寸标注。

除非用户明确要求后续阶段，暂不生成房间名称、面积、标题、楼梯、阳台、家具、柱体及其他非墙体构件。

## 必须遵守的执行顺序

1. 分析图片前，必须先运行 `scripts/processing.py`，读取 `result.json`、`overlay.png`、`masks/walls.png`、`masks/doors.png`、`masks/windows.png`、`masks/dimensions.png` 和 `masks/other.png`。
2. 先建立和校准墙体。用户提供的尺寸以及 `data.json` 中的墙厚候选值优先于仅从像素推算的厚度。
3. 先校验墙体：双线表达、短边墙厚准确、拓扑闭合、L 型/T 型/转角连接可靠、没有无意出头或破口，并且 DXF 审计通过。
4. 只有第 3 步通过后，才处理窗户候选，并将窗洞和窗框写入同一张 CAD 图纸。
5. 加入窗户后，必须重新执行墙体和窗户的完整校验。
6. 只有第 5 步通过后，才运行 `scripts/generate_doors.py`。门阶段必须读取上一阶段墙体和窗户图片、上一阶段 DXF 以及当前包含门的图片，通过阶段差分识别门型和位置。
7. 加入门后，必须再次校验墙体闭合、门洞无墙线重叠、门型和开启方向。静态脚本检查不能替代目标 CAD 中的实际显示检查；目标 CAD 可用时必须打开最终 DXF 复核。

## 图片证据和置信度规则

- `result.json` 和各类蒙版是识别证据，不能据此凭空创造几何。必须保留图片中的位置、方向和形状关系。
- 窗户候选必须同时得到窗户蒙版或叠加图支持，并且必须落在已经识别的墙体上。不得把尺寸线、文字笔画、门开启弧线或孤立标记当成窗户。
- 门候选必须得到门蒙版、阶段差分或叠加图支持，并且必须匹配已有墙洞或可验证的连续墙段。不得把锯齿墙线、窗框、尺寸线、文字笔画或孤立短线当成门。
- OCR 不可用或候选置信度较低时，保留为不确定候选，不得静默生成确定窗户，并在报告中说明原因。
- 用户明确提供的窗户尺寸优先；否则使用可靠尺寸链建立的比例换算，并把推算值标记为 `INFERRED`。

## 墙体阶段

- 墙体必须按真实墙厚生成两侧墙面，禁止只生成墙体中心线。
- 从 `data.json.wall_width` 读取允许的墙厚值，单位为毫米。读取程序可以兼容历史拼写 `wall_wdith`，但新配置必须使用 `wall_width`。
- 尺寸链中明确出现的墙厚优先于像素估计；其余墙体根据是否属于外墙以及配置候选值分类，并记录每个分配结果的来源和置信度。
- 墙体边界必须生成闭合 `LWPOLYLINE`。最终墙体图层禁止保留独立 `LINE`；任何无法重建为闭合多段线的墙体都必须停止输出并报告。

## 窗户阶段

窗户阶段的全部识别、几何计算、墙洞切割、窗框生成和窗户校验逻辑，统一写入 `scripts/generate_windows.py`。不得把窗户逻辑写入 `generate_doors.py`、`generate_by_pic.py` 或其他脚本；`generate_by_pic.py` 只负责墙体及墙体尺寸。

对每个接受的窗户候选，必须判断：所属墙体、水平或竖直方向、沿墙方向的开洞中心和净宽、穿墙方向的深度、窗户形状、置信度以及来源（`USER`、`DETECTED` 或 `INFERRED`）。

以下候选必须拒绝：不与墙体相交、同时落在多面墙上、与其他窗洞重叠、超出所属墙段范围，或无法可靠判断方向的候选。

### CAD 生成规则

- 新增 `WINDOWS` 图层用于窗框和窗户符号，墙体继续放在 `WALLS` 或 `INNER_WALLS` 图层。
- 必须在所属墙体边界中切出真实窗洞。禁止只把窗框符号覆盖在连续墙线上。
- 窗洞两端的两侧墙线必须封边，并重建为闭合墙体子多段线；不得留下悬空墙线、独立墙体 `LINE`、意外墙帽或重复重叠墙体。
- 图片中为矩形的窗户，生成与所属墙体方向一致的闭合矩形窗洞和窗框；图片明确显示非矩形窗户时，保留其轮廓并记录推算依据。
- 窗框必须位于窗洞内部，`WINDOWS` 图层不得放置尺寸标注或文字。

### 窗户校验规则

每个窗户必须满足：只属于一面墙、方向正确、开洞范围位于墙段内、不与其他窗洞重叠、两侧墙线在开洞处正确断开并连接、窗框闭合且无自相交。还必须通过 DXF 审计，并在目标 CAD 中确认看到的是真实开洞，而不是浮在连续墙体上的窗框。

## 门阶段

门阶段的全部识别、门型分类、墙洞匹配、墙洞切割、门扇和开启弧生成、推拉门生成及门校验逻辑，统一写入 `scripts/generate_doors.py`。不得把门逻辑写入 `generate_windows.py` 或 `generate_by_pic.py`。

门阶段必须以已校验的“墙体＋窗户”DXF 为基底，并优先使用当前含门图片与上一阶段不含门图片的差分。差分候选必须再次匹配墙体方向、墙厚和门洞端点，不能只凭差分外包框直接画门。

### 门型识别规则

- 平开门必须检测到可靠的四分之一圆门弧及与圆心连接的门扇直线。根据圆心、圆弧象限和门扇半径保留图片中的铰点、开启侧和开启方向，不得使用统一默认方向。
- 同一高度左右相邻且分别位于墙体两侧的两个门弧，可合并为一个双向平开门候选；它们共用同一门洞，但保留两条门扇和两条开启弧。
- 推拉门必须呈细长多线框，并包含两扇相互搭接的门板证据。单条墙线、窗框或短线不得按推拉门生成。
- 差分中的锯齿墙线、孤立短线和不具备门弧或多线框特征的区域必须记录为拒绝候选。

### 门的 CAD 生成规则

- 新增 `DOORS` 图层；门扇、门弧和推拉门板只能放在该图层。
- 必须在所属墙体中形成真实门洞。已有门洞应吸附到两端封边；连续墙段必须同时切断两侧墙面线并补齐门洞两端封边。
- 平开门使用 `LINE` 表达门扇，使用 `ARC` 表达开启弧；圆弧半径应等于门洞净宽或由可靠图片尺寸校准。
- 推拉门使用两个闭合 `LWPOLYLINE` 表达相互搭接的门板，方向必须与所属墙体一致。
- 门洞不得与窗洞或其他门洞重叠。门阶段结束后，墙体图层仍必须全部为闭合 `LWPOLYLINE`，不得残留墙体 `LINE`。

### 门的校验规则

每个门必须满足：门型正确、墙向正确、墙厚属于 `data.json.wall_width` 候选值、门洞范围位于墙段内、门洞内墙线重叠数为零、平开方向与图片一致、推拉门板闭合且搭接合理。还必须通过 DXF 审计，并在目标 CAD 中检查实际显示。

## 输入和输出约定

图片输入必须先运行 `scripts/processing.py`，并将预处理结果保存在输出目录附近。墙体-only 模式可以不生成窗户和门；窗户阶段和门阶段必须使用各自的显式脚本入口，不能让旧任务隐式生成后续构件。

带窗户任务的唯一生成脚本入口为 `scripts/generate_windows.py`。该脚本读取已完成的墙体结果和窗户图片，生成窗洞及窗框。

带门任务的唯一生成脚本入口为 `scripts/generate_doors.py`，调用格式为：

```powershell
python scripts/generate_doors.py 当前含门图片 上一阶段墙体窗户图片 上一阶段墙体窗户.dxf 输出带门.dxf --preprocessing-result 预处理结果.json
```

带窗户的结果应包含 `WALLS`、`INNER_WALLS`、`DIMENSIONS` 和 `WINDOWS` 图层。带门结果在此基础上增加 `DOORS` 图层；本阶段不得新增 `ROOM_NAMES` 或其他构件图层。

报告必须记录墙厚分配、窗户和门候选、接受或拒绝状态、门型、所属墙体、位置、尺寸、开启方向、置信度、门洞切割状态、拓扑校验结果，以及静态检查、预览检查和目标 CAD 实际检查是否完成。

## 停止条件

墙体拓扑未校验通过、墙体图层仍存在独立 `LINE`、任何墙体多段线未闭合、窗户或门无法定位到墙体、门型或开启方向存在歧义、门洞与窗洞重叠，或生成开洞需要凭空补充尺寸时，应停止并报告，不得猜测。只有程序校验和目标 CAD 可用时的实际显示检查都通过，才能宣称当前阶段生成完成。
