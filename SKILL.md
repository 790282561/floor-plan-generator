---
name: floor-plan-generator
description: 根据建筑户型图片或房间尺寸识别并生成建筑墙体，使用双线墙表达，并添加墙体尺寸及必要标注。
---

# 建筑户型墙体生成 Skill

当前阶段只实现“墙体及其标注”。门、窗、房间文字、面积、标题和其他建筑构件暂不生成。

## 1. 当前目标
### 1.1 首要目标
从户型图片或文字尺寸中识别建筑外轮廓和内部分隔墙，建立毫米坐标模型，生成具有实际厚度的双线墙体，并添加墙体相关尺寸标注。
### 1.2 暂不处理内容
本阶段禁止生成门洞、门扇、门开启弧线、窗洞、窗框、房间名称、面积、标题、楼梯、阳台、家具、柱体及其他非墙体构件。

## 2. 强制执行流程
### 2.1 输入解析与图片预处理
图片输入必须先执行 scripts/processing.py，再读取 result.json、overlay.png、masks/walls.png、masks/dimensions.png 和 masks/other.png。未安装 OCR 时，不得把 text=null 的候选区域当作确定尺寸。用户明确提供的墙厚和尺寸优先级最高。
### 2.2 建筑外轮廓识别
识别建筑整体边界、凹凸关系、转角和外墙连续关系。暂不识别非墙体附属构件。
### 2.3 墙体识别
识别墙体两侧边界、中心位置、长度、方向、厚度、端点和连接关系，并通过 overlay.png 和 walls.png 复核。不得将墙体直接转换为单根中心线。
### 2.4 墙体拓扑重建
处理水平墙、垂直墙、L 型、T 型、十字型连接和转角，保证墙体连续，无断裂、重复、悬空或无依据延伸。
### 2.5 墙体厚度确定
墙厚优先级为 USER > 图片明确标注 > 图片像素几何推算 > 默认值。默认外墙 240 mm、内墙 200 mm；推定值必须标记 INFERRED。
### 2.6 尺寸与比例校准
有明确尺寸时建立 scale = real_length / pixel_length，并用多个尺寸交叉校验。内部坐标统一使用 mm，原点为 (0,0)。
### 2.7 墙体几何生成
所有墙体必须生成有实际厚度的双线几何对象。可使用闭合墙体轮廓和 SOLID 填充，但不能只绘制中心线。
### 2.8 墙体标注生成
只添加建筑外轮廓总宽、总高、可靠的墙段长度和必要的墙厚标注。标注须与墙体保持间隙，推定尺寸不得标成精确实测值。
### 2.9 墙体结果校核
检查双线墙、墙厚、墙体连接、外轮廓闭合、重复墙、断墙、悬空墙和尺寸标注对应关系，并确认没有生成门窗或其他构件。

## 3. 数据模型
### 3.1 Wall
包含 id、start、end、thickness、type、source 和 confidence；最终必须根据厚度生成双线墙体。
### 3.2 WallAnnotation
包含 wall_id、kind、value、unit=mm、source 和 confidence。kind 可为 overall_width、overall_height、wall_length 或 wall_thickness。
### 3.3 UncertainWall
记录模糊墙段、候选几何、原因、来源和置信度，status 为 INFERRED；不得静默补画。

## 4. 信息可信度
### 4.1 USER
用户明确提供的墙厚、建筑尺寸或墙体位置，优先级最高。
### 4.2 DETECTED
从图片中明确识别出的墙体边界、墙段和尺寸。
### 4.3 INFERRED
由比例、连续性、对称关系或默认参数推定的信息，必须显式标记。
### 4.4 Confidence
低置信度或重叠候选不得作为确定墙体直接绘制；必要时保留为 UncertainWall。优先级为 USER > DETECTED > INFERRED。

## 5. 墙体几何规则
### 5.1 双线墙规则
任何 Wall 都必须转换为具有实际厚度的双线几何对象，禁止单根线条代替墙体。
### 5.2 墙体连接规则
正确处理 L、T、十字连接和转角，不得断裂、错位、重复或悬空。
### 5.3 外轮廓规则
外墙应形成连续闭合的建筑外轮廓，不得随意增加复杂结构。
### 5.4 内墙规则
内墙应连接到已确认的外墙或其他内墙，并保持合理墙厚；没有依据时不得随意延长。
### 5.5 墙体重叠规则
相邻墙体应正确相交或合并，避免异常加厚、墙体穿越和悬空。

## 6. CAD绘制规则
### 6.1 图层
本阶段只使用 WALLS、INNER_WALLS 和 DIMENSIONS 图层；不得绘制 DOORS、WINDOWS、ROOM_NAMES 内容。
### 6.2 墙体
使用实际墙厚生成闭合墙体轮廓，必要时使用 SOLID 填充，正式结果必须能看出两条墙体边界线。
### 6.3 墙体标注
尺寸线放置在建筑外轮廓外侧并保持间隙，至少标注可确认的总宽、总高或关键墙段长度。
### 6.4 暂停的绘制内容
本阶段不得调用或输出门、窗、房间标签、面积、标题和其他建筑构件。

## 7. 纠错机制
采用“识别—建模—校核—局部修正—再次校核”闭环。无法可靠判断时保留候选并标记 INFERRED，不得凭空制造复杂结构。

## 8. 输入方式与使用示例
### 图片输入
请根据这张户型图只生成墙体和墙体尺寸标注，暂时不要生成门窗、房间名称、面积和标题。
### 文字输入
生成墙体平面图：外轮廓宽度 12000，外轮廓高度 9000，外墙厚度 240，内墙厚度 200，只生成双线墙和墙体尺寸标注。

## 9. 默认参数
单位为 mm；外墙厚度 240；内墙厚度 200；标注总宽、总高及可靠的关键墙段尺寸。图片或用户数据可靠时优先使用可靠数据。

## 10. 禁止事项
- 使用单线代替墙体
- 未执行图片预处理就直接生成
- 把低置信度候选当成确定墙体
- 忽略用户指定墙厚或明确尺寸
- 随意改变建筑外轮廓和墙体拓扑
- 生成门、窗、房间名称、面积、标题或其他非墙体构件
- 执行 draw_doors、draw_windows、add_room_labels 或 add_title
- 将推定尺寸标记为精确实测尺寸
