---
name: floor-plan-generator
description: 先生成并校准户型墙体，再根据户型图片识别窗户位置和形状，生成真实窗洞及窗框 CAD 图形。
---

# 户型墙体与窗户生成

本 Skill 用于根据户型图片或明确尺寸生成可编辑 CAD 图纸。流程必须分阶段执行：先建立并校验墙体，再在墙体基础上识别和生成窗户。

## 当前范围

支持以下内容：

- 具有真实墙厚的双线墙体；
- 全部由闭合 `LWPOLYLINE` 表达的墙体边界；
- 根据图片证据生成窗洞和窗框；
- 总体尺寸及可靠的墙体尺寸标注。

除非用户明确要求后续阶段，暂不生成门、门扇、门开启弧线、房间名称、面积、标题、楼梯、阳台、家具、柱体及其他非墙体构件。

## 必须遵守的执行顺序

1. 分析图片前，必须先运行 `scripts/processing.py`，读取 `result.json`、`overlay.png`、`masks/walls.png`、`masks/windows.png`、`masks/dimensions.png` 和 `masks/other.png`。
2. 先建立和校准墙体。用户提供的尺寸以及 `data.json` 中的墙厚候选值优先于仅从像素推算的厚度。
3. 先校验墙体：双线表达、短边墙厚准确、拓扑闭合、L 型/T 型/转角连接可靠、没有无意出头或破口，并且 DXF 审计通过。
4. 只有第 3 步通过后，才处理窗户候选，并将窗洞和窗框写入同一张 CAD 图纸。
5. 加入窗户后，必须重新执行墙体和窗户的完整校验。静态脚本检查不能替代目标 CAD 中的实际显示检查；目标 CAD 可用时必须打开最终 DXF 复核。

## 图片证据和置信度规则

- `result.json` 和各类蒙版是识别证据，不能据此凭空创造几何。必须保留图片中的位置、方向和形状关系。
- 窗户候选必须同时得到窗户蒙版或叠加图支持，并且必须落在已经识别的墙体上。不得把尺寸线、文字笔画、门开启弧线或孤立标记当成窗户。
- OCR 不可用或候选置信度较低时，保留为不确定候选，不得静默生成确定窗户，并在报告中说明原因。
- 用户明确提供的窗户尺寸优先；否则使用可靠尺寸链建立的比例换算，并把推算值标记为 `INFERRED`。

## 墙体阶段

- 墙体必须按真实墙厚生成两侧墙面，禁止只生成墙体中心线。
- 从 `data.json.wall_width` 读取允许的墙厚值，单位为毫米。读取程序可以兼容历史拼写 `wall_wdith`，但新配置必须使用 `wall_width`。
- 尺寸链中明确出现的墙厚优先于像素估计；其余墙体根据是否属于外墙以及配置候选值分类，并记录每个分配结果的来源和置信度。
- 墙体边界必须生成闭合 `LWPOLYLINE`。最终墙体图层禁止保留独立 `LINE`；任何无法重建为闭合多段线的墙体都必须停止输出并报告。

## 窗户阶段

窗户阶段的全部识别、几何计算、墙洞切割、窗框生成和窗户校验逻辑，统一写入 `scripts/generate_doors.py`。虽然文件名含有 `doors`，但按当前项目约定，该文件是窗户阶段的唯一实现入口。不得把窗户逻辑继续写入 `generate_windows.py`、`generate_by_pic.py` 或其他脚本；`generate_by_pic.py` 只负责墙体及墙体尺寸。

对每个接受的窗户候选，必须判断：所属墙体、水平或竖直方向、沿墙方向的开洞中心和净宽、穿墙方向的深度、窗户形状、置信度以及来源（`USER`、`DETECTED` 或 `INFERRED`）。

以下候选必须拒绝：不与墙体相交、同时落在多面墙上、与其他窗洞重叠、超出所属墙段范围，或无法可靠判断方向的候选。

### CAD 生成规则

- 新增 `WINDOWS` 图层用于窗框和窗户符号，墙体继续放在 `WALLS` 或 `INNER_WALLS` 图层。
- 必须在所属墙体边界中切出真实窗洞。禁止只把窗框符号覆盖在连续墙线上。
- 窗洞两端的两侧墙线必须封边，并重建为闭合墙体子多段线；不得留下悬空墙线、独立墙体 `LINE`、意外墙帽或重复重叠墙体。
- 图片中为矩形的窗户，生成与所属墙体方向一致的闭合矩形窗洞和窗框；图片明确显示非矩形窗户时，保留其轮廓并记录推算依据。
- 窗框必须位于窗洞内部，`WINDOWS` 图层不得放置尺寸标注或文字。

### 窗户校验规则

每个窗户必须满足：只属于一面墙、方向正确、开洞范围位于墙段内、不与其他窗洞重叠、两侧墙线在开洞处正确断开并连接、窗框闭合且无自相交。还必须通过 DXF 审计，并在目标 CAD 中确认看到的是真实开洞，而不是浮在连续墙体上的窗框。

## 输入和输出约定

图片输入必须先运行 `scripts/processing.py`，并将预处理结果保存在输出目录附近。墙体-only 模式可以不生成窗户；带窗户的阶段必须使用显式选项（例如 `--with-windows`），不能让旧的墙体-only 任务隐式生成窗户。

带窗户任务的唯一生成脚本入口为 `scripts/generate_doors.py`。该脚本应读取已完成的墙体结果以及 `processing.py` 输出的窗户候选，在墙体基础上完成窗洞和窗框生成。

带窗户的结果应包含 `WALLS`、`INNER_WALLS`、`DIMENSIONS` 和 `WINDOWS` 图层。本阶段不得新增 `DOORS`、`ROOM_NAMES` 或其他构件图层。

报告必须记录墙厚分配、窗户候选、接受或拒绝状态、所属墙体、位置、尺寸、形状、置信度和来源、拓扑校验结果，以及静态检查、预览检查和目标 CAD 实际检查是否完成。

## 停止条件

墙体拓扑未校验通过、墙体图层仍存在独立 `LINE`、任何墙体多段线未闭合、窗户无法定位到墙体、图片形状存在歧义，或生成窗洞需要凭空补充尺寸时，应停止并报告，不得猜测。只有程序校验和目标 CAD 可用时的实际显示检查都通过，才能宣称窗户生成完成。
