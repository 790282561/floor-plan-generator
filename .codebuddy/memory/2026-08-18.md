# 2026-08-18

- 重写 `scripts/generate.py`：移除 ezdxf 后端，改为通过 COM (`win32com.client.GetActiveObject`) 连接正在运行的 CAD（ProgID 依次尝试 AutoCAD.Application / Gcad.Application / ZWCAD.Application），将户型线稿直接绘制到当前活跃文档模型空间，不再生成 DXF/DWG 文件。绘制封装在 `_add_lwpolyline/_add_line/_add_arc/_add_text`（角度转弧度、文字居中对齐 Alignment=10），图层 WALLS/INNER_WALLS/DOORS/WINDOWS/DIMENSIONS/ROOM_NAMES/TEXT 已存在则复用，结束后 ZoomExtents+Regen，不调用保存。`FloorPlanConfig` 保留 output_filename/output_dir 字段仅为兼容旧 JSON。
- `SKILL.md` 新增 §10.1「输出方式：直接绘制到当前 CAD 活跃文档」，与脚本行为对齐。
- 依赖变化：ezdxf 不再需要，改为需要 pywin32。
- 用户要求：图纸文字严禁出现 ??? 乱码。`generate.py` 新增 `_setup_text_style()`，创建专用样式 `FP-TEXT`（优先 gbenor.shx+gbcbig.shx 国标大字体，回退 simsun.ttc 等 TrueType 中文字体），`_add_text` 强制 `t.StyleName = self.text_style`；SKILL.md 新增 §10.2 文字字体要求并在 §15 禁止事项加入乱码禁令。
